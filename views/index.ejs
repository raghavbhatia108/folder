<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Posts</h1>
   <% for(post of posts){ %>
   <div>
    <h3 class="user">@<%= post.username %></h3>
    <h3><%= post.content %></h3>
    <a href="http://localhost:8080/posts/<%= post.id %>">
    <button>See in detail</button></a>
    <br>
    <br>
   </div>

   <% } %>

   <a href="http://localhost:8080/posts/new"><button>add new post</button>
   </a>
</body>
</html>